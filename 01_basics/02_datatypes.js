// code readability should be high


let name = "alif"
let age = 21;
let isActive = false;

// number => 2 to power 53
// bigint 
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique 

// object

console.table({
    "Name": (typeof name),
    "Age" : (typeof age),
    "isActive": (typeof isActive)
    })

console.log(typeof "Alif")

console.log(` type of Null: ${typeof null} `) // object (typeof null)